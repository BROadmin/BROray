# BROray 3.0.1-r07 — Stable

- Публичный release: **3.0.1-r07**
- Пакет OPKG: **3.0.0-r14**
- Технический кандидат: **3.0.1-r07c01**
- WebUI: **WebUI-3.0.1-r07c01**
- Обновитель: **broray-updater/5**
- Архитектура: **aarch64-3.10**
- Дата публикации: **8 сентября 2026 года**

`r07c01` опубликован в Stable из application-байтов, прошедших локальную, терминальную, пользовательскую WebUI и визуальную проверку. Updater platform побайтово не изменялась.

Один версионный `INSTALL-ON-ROUTER.sh` обслуживает оба пользовательских сценария. Если BROray ещё не установлен, он сразу устанавливает целевое приложение `3.0.1-r07c01`; пакет OPKG `3.0.0-r14` используется только как регистрационный и lifecycle-якорь. Промежуточное приложение `r14c68` не устанавливается и не запускается. На установленной ветке `3.0.0-r1` и новее применяется прежний универсальный алгоритм обновления.

Универсальный алгоритм обновления сохранён: для любой исправной установки от `3.0.0-r1` сравниваются только установленный и текущий подписанный `releaseId`. Более старая версия обновляется, равная завершается без замены приложения, более новая не понижается.

## Исправлено

- При обновлении подписки изменение только отображаемого имени активного сервера теперь синхронизируется с именем управляемого ProxyN.
- Если точное согласование ProxyN после обновления подписки нельзя подтвердить, операция откатывается и сохраняет последнее согласованное состояние.
- После активации сервера статус Keenetic обновляется сразу и не показывает имя предыдущего активного сервера из кэша.
- Внутренняя навигация WebUI и локальные ресурсы используют идентификатор текущей сборки, поэтому браузер не смешивает страницы разных кандидатов.
- Монитор соединения автоматически восстанавливается после неожиданной остановки.
- Устаревший PID и PID постороннего процесса не принимаются за работающий монитор; посторонний процесс не завершается.
- Состояние соединения публикуется атомарно, поэтому пустой, оборванный или некорректный `connection-status.json` не становится авторитетным.
- Завершённая ручная или плановая пакетная проверка серверов публикует единый итоговый снимок для Главной. Промежуточный снимок больше не оставляет ложное предупреждение об устаревшем качестве.
- Неудачная, но завершённая проверка сервера остаётся свежим результатом «недоступен», а не ошибочно помечается как устаревшая.
- Сообщения Главной явно указывают, что счётчик выбранных серверов относится к DNS-over-TLS.
- Установка готового набора Wikipedia сохраняет актуальную конфигурацию Keenetic перед построением транзакционного плана; все 12 маршрутов корректно распознаются после применения.
- На карточках DNS-over-TLS удалён повторяющийся индикатор; установленная запись имеет один статус «Установлен в Keenetic».
- Срок свежести качества серверов учитывает настроенный интервал автопроверки и ограниченное время последовательного обхода.
- Запуск целевого app-slot во время обновления не запускает конкурирующее согласование интерфейса, пока блокировкой владеет updater-v5.
- Старт и перезапуск BROray гарантируют запуск планировщика автоматического обновления подписок, в том числе после обновления из состояния с остановленной старой службой.

## Добавлено

- Десятый готовый маршрутный набор Wikipedia. Исходный BAT загружается и разбирается как недоверенные данные, никогда не выполняется как команда.
- Полноширинная раскрывающаяся карточка «Доступ к WebUI через KeenDNS» на странице Keenetic.
- Для управляемого адреса `https://broray.<KeenDNS-домен>/` доступны просмотр состояния, включение, отключение, открытие и копирование.
- Операции с адресом используют существующую проверку владения, не перезаписывают чужой HTTP Proxy и не меняют регистрацию или режим KeenDNS роутера.
- Прямая чистая установка текущего Stable без внутреннего перехода через приложение `r14c68`.

## Проверено

- application archive: SHA-256 `c8d7ac9d2c3a2f82c911700ba8f9e4a276f3f1ded13bf61f33333161a0f160e9`;
- updater-v5: SHA-256 `a3c094b3a5e82ac82be7ec8b53c90f4023351a94eb6ba945b99501c0876e2c4a`, без изменений;
- автоматическое восстановление монитора, защита PID и атомарная регенерация состояния;
- ручная и плановая проверка качества, включая итоговый снимок без ложного `QUALITY_STALE`;
- маршрут Wikipedia и счётчик десяти готовых наборов;
- фактическое состояние Wikipedia после установки: 12 из 12 управляемых маршрутов;
- DNS-over-TLS: 3 из 3 выбранных записей установлены, состояние однозначно согласовано с runtime;
- ручная и автоматическая проверка качества: 7 из 7 серверов доступны, устаревших результатов нет;
- автоматическое обновление тестовой подписки: получено 7, принято 7, отклонено 0, источник запуска — `automatic`;
- восстановление остановленного планировщика подписок после обновления и после `S24broray restart`;
- включение, отключение, открытие и копирование KeenDNS-адреса при сохранении локального доступа;
- страницы WebUI на телефоне, планшете и компьютере, без горизонтального переполнения и ошибок консоли;
- полное штатное удаление без сохранения архива и чистая установка публичного Stable на `KN-2710`: `BRORAY_INSTALL=PASS`, `INTERMEDIATE_APPLICATION_RELEASE=NONE`, `STABLE=YES`;
- после чистой установки проверены OPKG-регистрация, Xray, `Proxy0`, updater, монитор, WebUI и все восемь страниц;
- импортировано 62 из 62 серверов пользовательской подписки; для активного сервера с именем `🇩🇪 Франкфурт-на-Майне, Германия, Extra` имя ProxyN осталось точным после повторного обновления подписки;
- полный физический аудит всех страниц, функций и кнопок WebUI: PASS; опасные операции проверены до подтверждения и отменены без изменения пользовательских данных;
- итоговое состояние тестового роутера: Xray и обязательные службы исправны, 7 из 7 серверов, 3 из 3 DNS-over-TLS-записей, 12 из 12 маршрутов Wikipedia;
- повторный публичный Stable installer на уже установленном `r07c01`: `UPDATE_DECISION=already-current`, `RELEASE_MUTATION=NONE`, `ENVIRONMENT_RECONCILED=YES`.

## Обязательное действие после обновления

После обновления очистите кэш браузера и заново откройте WebUI. На компьютере обновите страницу сочетанием `Ctrl+R`; на мобильном устройстве очистите кэш сайта в настройках браузера и перезагрузите страницу. Это исключает показ HTML старой версии из локального кэша браузера.

## Контрольные суммы Stable

- `release.json`: `f5017edfdcba68f183c7195071897ef85e634b045d9009201ca94ec358d8cfcf`
- `release.json.minisig`: `00e09b649e6fb944b7e7fe7123edf619e5173a46c75a4058b5664960d8f0b3f6`
- `INSTALL-ON-ROUTER.sh`: `82d102e227fdaef2ea94708de8c540314e8c6b2cd89ab5a188cfcc771c37ff5d`
- direct OPKG package: `cf7704d6a017703f0d51a23f73d38f23703ea69c829189a79b7175d238e8adf3`
- direct clean bootstrap: `10bf21c1031d26569bfa2247097b1f2389958f0b5048bb2fafaa5292365c9bf9`
- application archive: `c8d7ac9d2c3a2f82c911700ba8f9e4a276f3f1ded13bf61f33333161a0f160e9`
- updater platform: `eaf2eafb62d1b108fe576d1fda09ae7a5d15ad90f71272abc23f321d397611b0`

Stable index: <https://api.brovibe.cloud/releases/stable/broray/release.json>

Версионный установщик: <https://api.brovibe.cloud/releases/stable/broray/3.0.1-r07/INSTALL-ON-ROUTER.sh>
