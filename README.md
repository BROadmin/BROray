# BROray

BROray — менеджер Xray с WebUI для маршрутизаторов Keenetic.

Проект работает в среде Entware и использует штатные механизмы KeeneticOS: OPKG, CLI/`ndmc`, HTTP Proxy и интерфейс Proxy. Скрипты рассчитаны на BusyBox `ash`.

## Возможности

- управление серверами Xray через WebUI и CLI;
- импорт отдельных подключений и подписок;
- выбор и переименование серверов;
- создание и обслуживание интерфейса Keenetic;
- запуск, остановка, перезапуск и проверка Xray;
- диагностика состояния;
- локальный WebUI и публикация через KeenDNS HTTP Proxy;
- маршрутизация настраивается стандартными способами через Keenetic (bat-файлы можно собрать самостоятельно или скачать готовые https://github.com/RockBlack-VPN/ip-address/tree/main).

## Текущая версия

BROray 2.0.0.

Проверено на Keenetic Ultra KN-1811 с KeeneticOS 5.1.1 Preview, BusyBox 1.37.0 и Entware `aarch64-3.10`.

## Установка

Требуется установленный Entware и доступ к терминалу Keenetic под `root`.

```sh
opkg update &&
opkg install ca-bundle ca-certificates wget-ssl

wget -qO /tmp/broray-opkg.sh \
    https://api.brovibe.cloud/releases/opkg.sh &&
ash /tmp/broray-opkg.sh
```

После установки WebUI обычно доступен по адресу:

```text
http://192.168.1.1:8080/
```

Фактический LAN-адрес определяется автоматически.

## Обновление

```sh
opkg update &&
opkg upgrade broray
```

## Удаление

```sh
opkg remove broray
```

После удаления пакета пользовательские данные и резервные копии могут сохраняться в `/opt/broray`. Перед их ручным удалением убедитесь, что нужные подписки и настройки сохранены отдельно.

## CLI

```sh
broray help
broray list
broray current
broray status
broray test
broray restart
```

## Исходный код и релизы

Каталог `root/` повторяет пути устанавливаемой файловой системы. Бинарный файл Xray не хранится в исходном дереве репозитория; он добавляется при сборке пакета из официального выпуска Xray-core.

Готовые стабильные пакеты распространяются через OPKG-репозиторий BROray.

## Важно

- Для проверки shell-скриптов на Keenetic используйте `ash -n`, а не `/bin/sh -n`.
- Не публикуйте файлы из `servers/`, `subscriptions/`, `run/`, `backup/` и `backups/`: они могут содержать адреса серверов, UUID и ссылки подписок.
- Перед применением маршрутизации убедитесь, что использование решения соответствует правилам вашей сети и применимому законодательству.

## Лицензия

Лицензия проекта будет добавлена до открытия репозитория для публичного доступа. Пока репозиторий не содержит явной лицензии, все права на код сохраняются за правообладателем.

