# BROray WebUI — Stage 3

Build: `20260725-webui-home-xray-03`

Полностью перенесены на `allpage.css`:

- Главная;
- Xray.

Для этих страниц отключены `home.css` и `app.css`. Xray использует отдельный
модуль `xray.js`, а навигацией и выходом управляет единая статическая оболочка.
Рабочий `/opt/broray/web-new` этим этапом не изменяется.
