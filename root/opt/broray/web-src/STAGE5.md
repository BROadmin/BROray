# BROray WebUI — Stage 5

Build ID: `20260725-webui-servers-routes-05`

Перенесены в единый `allpage.css` и общую статическую оболочку:

- Главная;
- Xray;
- Подписки;
- Keenetic;
- Серверы;
- Маршруты.

Страницы «Серверы» и «Маршруты» используют утверждённое двухуровневое представление: краткое состояние и основные действия видны сразу, технические сведения раскрываются по запросу. Системные `alert`, `confirm` и `prompt` не используются.

Рабочий `/opt/broray/web-new` этим этапом не переключается.
