# Stage 11 — desktop layout

Desktop-only layout corrections approved by the user:

- Home cards: two columns.
- Server summary metrics: under the active connection, four in one row.
- Routes: Details button below Check and Restore/Install.
- Keenetic: comparison, diagnostics and technical data in one column.
- Xray metrics: one row below the primary Xray card.
- BROray update controls: separate Update card below installed version; protocol list in one column on desktop.

Mobile layout remains unchanged except the BROray update card is visually joined to the installed-version card.
